# nook-ai 本地密钥文件模板。复制为同目录下的 .env 并填入真实值（.env 不提交）：
#   cp nook-ai/.env.example nook-ai/.env
# 然后在 .env 里填入 DeepSeek API Key。application.yml 通过 spring.config.import 在启动时加载本文件。
# 申请地址：https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here
